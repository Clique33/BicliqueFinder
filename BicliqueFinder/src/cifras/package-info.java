/**
 * Este pacote inclui a classe abstrata <code>Cipher</code>, que possui todos os
 * métodos básicos para as cifras a serem utilizadas no programa. Atualmente,
 * as classes filhas de <code>Cipher</code> são as que implementam as cifras
 * Rijndael e Serpent.
 */
package cifras;
